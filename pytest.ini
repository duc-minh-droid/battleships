[pytest]
testpaths = .
python_files = test_*.py
# Markers used by the coursework test suite (pytest-dependency / pytest-order);
# registered so the suite runs cleanly without those plugins installed.
markers =
    dependency: pytest-dependency marker used by test_students.py
    order: pytest-order marker used by test_students.py
